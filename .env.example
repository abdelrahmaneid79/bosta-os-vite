# Supabase — copy to .env and fill in. Only the ANON key belongs in the client.
# Never put the service_role key here; RLS secures the anon key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# When unset, the app runs against the isolated mock layer (src/lib/mock).
